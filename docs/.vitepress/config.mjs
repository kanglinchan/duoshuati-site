import { defineConfig } from 'vitepress'

export default defineConfig({
  // 绑定自定义域名 www.duoshuati.com，使用根路径
  base: '/',
  title: '多刷题',
  description: '小书装口袋，越刷越明白',
  themeConfig: {
    // 顶部导航等按需补充
  }
})
